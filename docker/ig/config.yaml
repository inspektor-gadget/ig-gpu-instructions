operator:
  otel-profiles:
    exporters:
      my-profiles-exporter:
        exporter: otlp-grpc
        compression: gzip
        # pyroscope
        endpoint: "pyroscope:4040"
        insecure: true
